USE `trotsa`;
DROP procedure IF EXISTS `delete_applicant`;

DELIMITER $$
USE `trotsa`$$
CREATE PROCEDURE `delete_applicant` (ID_applicant int)
BEGIN
DELETE FROM `trotsa`.`applicants` WHERE (`ID_applicants` = ID_applicant);
END$$

DELIMITER ;

